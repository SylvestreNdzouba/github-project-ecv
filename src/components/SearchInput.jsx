// eslint-disable-next-line react/prop-types
const SearchInput = ({ onChange }) => {
    return <input type="text" id="githubName" name="githubName" onChange={onChange} />;
};

export default SearchInput;
